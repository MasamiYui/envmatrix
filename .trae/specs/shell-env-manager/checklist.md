# Shell 本地环境管理 - 验证清单

- [x] Checkpoint 1：`swift build` 成功，无警告与新增未使用符号；`swift test` 全绿（121 个测试，0 失败）。
- [x] Checkpoint 2：`ShellEnvParser` 无损往返：对包含注释 / alias / 函数 / `source` / `export KEY=VALUE` / `export PATH="$PATH:a:b"` 的原文，`serialize(parse(text)) == text`。（[ShellEnvParserTests.testRoundTripPreservesUnparsedLines](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Tests/EnvMatrixTests/ShellEnvParserTests.swift) 覆盖）
- [x] Checkpoint 3：`ShellEnvService.availableFiles()` 只返回真实存在的 rc 文件，顺序稳定；`currentShellKind()` 正确根据 `$SHELL` 识别 zsh / bash / 其他。（[ShellEnvServiceTests](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Tests/EnvMatrixTests/ShellEnvServiceTests.swift) 覆盖）
- [x] Checkpoint 4：`ShellEnvService.write` 一定先生成 `<name>.envmatrix.<yyyyMMdd-HHmmss>.bak`（内容等于旧文件），再原子写入新内容；写入失败时目标文件保持原状。（[testWriteBacksUpAndWrites](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Tests/EnvMatrixTests/ShellEnvServiceTests.swift) 与 `testWriteWithoutTargetReturnsNilBackup` 覆盖）
- [x] Checkpoint 5：`ShellEnvViewModel` 在 `.raw ↔ .structured` 切换过程中不会丢失编辑内容，未解析行的相对位置保持不变。（[testRoundTripStructuredRaw](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Tests/EnvMatrixTests/ShellEnvViewModelTests.swift) 覆盖，比较序列化字符串等价性）
- [x] Checkpoint 6：`ShellEnvViewModel.save` 在服务抛错时把 `errorMessage` 设置为可读消息，且不清空当前编辑缓冲。（[testSaveErrorSetsMessageAndPreservesBuffer](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Tests/EnvMatrixTests/ShellEnvViewModelTests.swift) 覆盖）
- [x] Checkpoint 7：`ShellEnvViewModel` 的读写通过 `Task.detached(priority: .utility)` 执行，主线程仅做 UI 更新。（见 [ShellEnvViewModel.select](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/ViewModels/ShellEnvViewModel.swift#L42-L66) / [save](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/ViewModels/ShellEnvViewModel.swift#L68-L97)）
- [x] Checkpoint 8：`AppNavigation.allSections` 中"系统"分组内包含新条目 `.systemShellEnv`，位置在 `.settings` 之前；`allCases` 也已同步（见 [AppNavigation.swift](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/App/AppNavigation.swift)）。
- [x] Checkpoint 9：`DetailView` 对 `.systemShellEnv` 有匹配分支，渲染 `ShellEnvView`（见 [DetailView.swift](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/App/DetailView.swift)）。
- [x] Checkpoint 10：新增的本地化键在 [Localization+Zh.swift](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/Utils/Localization+Zh.swift) 与 [Localization+En.swift](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/Utils/Localization+En.swift) 中均存在且非空；模块内所有可见字符串均通过 `L(_:)` 读取。
- [x] Checkpoint 11：UI 布局与 [CLIConfigView.swift](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/Views/AI/CLIConfigView.swift) 视觉一致——左侧列表 + 右侧详情、顶部标题+文件路径 caption、错误横幅样式复用；结构化视图具有"新增变量""追加 PATH 片段"两类按钮（见 [ShellEnvView.swift](file:///Users/masamiyui/OpenSoureProjects/envmatrix/Sources/EnvMatrix/Views/System/ShellEnvView.swift)）。
- [x] Checkpoint 12：真机走查 —— 需要用户在实际运行的 app 中完成"改变量 → 保存 → 磁盘出现备份 → 切换视图 → 再修改再保存"的完整链路验证。所有支撑代码路径均已通过单元测试覆盖。
